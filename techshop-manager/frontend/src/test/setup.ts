/**
 * Vitest global test setup.
 * Imports @testing-library/jest-dom to extend Vitest's expect with DOM matchers
 * (toBeInTheDocument, toHaveStyle, toBeDisabled, etc.)
 */
import '@testing-library/jest-dom';
